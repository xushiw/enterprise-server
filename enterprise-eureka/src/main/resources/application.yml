eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true  # 默认不注册自己
    fetch-registry: false

spring:
  application:
    name: eureka

server:
  port: 8761

